<<<<<<< HEAD
apiVersion: v1  # versão da API do Kubernetes para recursos core (Pod)
kind: Pod  # tipo do recurso sendo definido
metadata:  # metadados que identificam o objeto (nome, labels, annotations)
  creationTimestamp: null  # timestamp de criação (preenchido pelo servidor quando criado)
  labels:  # rótulos para seleção/organização (úteis em selectors e serviços)
    run: wisomar  # rótulo chave=valor usado para identificar/selecionar esse Pod
  name: wisomar-2  # nome do Pod no namespace atual
spec:  # especificação desejada do Pod — containers, políticas e comportamentos
  containers:  # lista de containers que compõem esse Pod
  - image: nginx  # imagem do container (repositório:tag)
    name: wisomar  # nome do container dentro do Pod (referência local)
    ports:  # portas expostas declaradas pelo container
    - containerPort: 80  # porta interna do container (útil para Services e mapping)
    resources: {}  # requests/limits de CPU/memória (vazio = não especificado)
  dnsPolicy: ClusterFirst  # política de DNS (ClusterFirst é a padrão para clustesn)
  restartPolicy: Always  # política de reinício dos containers (Always/OnFailure/Never)
status: {}  # campo de status preenchido pelo kubelet/servidor (estado atual)


=======
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: wisomar
  name: wisomar-2
spec:
  containers:
  - image: nginx
    name: wisomar
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
>>>>>>> 0d01d9978809450b2b7d9796480de4e420ccd412
